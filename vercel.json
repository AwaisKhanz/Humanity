{
  "buildCommand": "pnpm install --prefer-offline --frozen-lockfile && pnpm build",
  "installCommand": "pnpm install --prefer-offline --frozen-lockfile",
  "outputDirectory": ".next",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}